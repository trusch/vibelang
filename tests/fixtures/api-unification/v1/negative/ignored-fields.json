{
  "schema": "https://vibelang.org/schemas/v1-negative-fixture/1",
  "defect": "ignored_fields",
  "expected_current_outcome": "accepted_without_named_effect_or_structured_diagnostic",
  "cases": [
    "PATCH /transport: quantization_beats",
    "PUT group/voice/pattern/effect param: fade_beats",
    "POST /voices: gain, sample, sfz",
    "PATCH /voices/:id: synth_name, polyphony, gain",
    "POST /patterns: pattern_string",
    "PATCH /patterns/:id: pattern_string",
    "pattern start/stop: quantize_beats",
    "POST /melodies event: frequency, params",
    "POST /melodies: melody_string, params",
    "PATCH /melodies/:id: melody_string, lanes, params",
    "melody start/stop: quantize_beats",
    "POST /sequences clip: fade or unknown type",
    "POST /sequences clip: once",
    "MIDI note-off: velocity",
    "MIDI stop-recording: quantize",
    "WebSocket input: unknown action, malformed JSON, non-text input",
    "WASM tick before init",
    "WASM stop before init",
    "WASM bridge resolved value",
    "Rhai Fade.apply",
    "Rhai Record.immediate through Record.apply",
    "Rhai Record.apply returned sample insertion",
    "Rhai Sequence.clip Pattern dependency materialization",
    "Rhai Sequence.clip nested Sequence dependency materialization"
  ],
  "source_assertions": [
    { "path": "crates/vibelang-http/src/models.rs", "contains": "pub quantization_beats: Option<f64>" },
    { "path": "crates/vibelang-http/src/models.rs", "contains": "pub fade_beats: Option<f64>" },
    { "path": "crates/vibelang-http/src/models.rs", "contains": "pub pattern_string: Option<String>" },
    { "path": "crates/vibelang-http/src/models.rs", "contains": "pub melody_string: Option<String>" },
    { "path": "crates/vibelang-http/src/models.rs", "contains": "pub once: Option<bool>" },
    { "path": "crates/vibelang-http/src/routes/midi.rs", "contains": "pub quantize: Option<f64>" },
    { "path": "crates/vibelang-rhai/src/api/sequence.rs", "contains": "pub fn apply(self) -> Self" },
    { "path": "crates/vibelang-rhai/src/api/sequence.rs", "contains": "pub fn clip_pattern(mut self, range: Range<f64>, pattern: Pattern) -> Self" },
    { "path": "crates/vibelang-rhai/src/api/sequence.rs", "contains": "pub fn clip_sequence(mut self, range: Range<f64>, seq: Sequence) -> Self" },
    { "path": "crates/vibelang-rhai/src/api/recording.rs", "contains": "start_beat: None, // Will be calculated by the runtime" },
    { "path": "crates/vibelang-wasm/src/lib.rs", "contains": "if let Some(runtime) = &mut self.runtime" }
  ]
}
