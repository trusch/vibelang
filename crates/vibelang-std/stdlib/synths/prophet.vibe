// Sequential Circuits Prophet-5 Emulation
// Classic 1978 | Character: Warm, rich, iconic analog
//
// The first practical polyphonic synthesizer.
// Known for its warm, rich sound and poly-mod.

// Prophet Pad - Lush analog pad
define_synthdef("prophet_pad", |builder| {
    builder
        .param("freq", 220.0) // units: Hz, range: 20..20000, default: 220.0. Pitch frequency in Hz; higher raises the note or resonator center.
        .param("amp", 0.4) // units: linear, range: 0..1, default: 0.4. Output level multiplier; higher is louder.
        .param("gate", 1.0) // units: trigger, range: 0..1, default: 1.0. Gate or trigger input; positive edges fire the event or advance the clock.
        .param("spread", 0.5) // units: normalized, range: 0..1, default: 0.5. Spread width; higher values distribute energy across a wider range.
        .body(|freq, amp, gate, spread| {
            let env = envelope()
                .asr(0.4, 1.0, 0.5)
                .gate(gate)
                .cleanup_on_finish()
                .build();

            // Two oscillators with spread
            let detune = spread * 0.01;
            let osc_a = saw_ar(freq * (1.0 - detune));
            let osc_b = saw_ar(freq * (1.0 + detune));

            // Sub oscillator
            let sub = sin_osc_ar(freq * 0.5) * 0.3;

            let mix = (osc_a + osc_b) * 0.4 + sub;

            // Warm lowpass
            let filtered = lpf_ar(mix, 2500.0);

            filtered * env * amp
        })
});

// Prophet Brass - Classic synth brass
define_synthdef("prophet_brass", |builder| {
    builder
        .param("freq", 220.0) // units: Hz, range: 20..20000, default: 220.0. Pitch frequency in Hz; higher raises the note or resonator center.
        .param("amp", 0.5) // units: linear, range: 0..1, default: 0.5. Output level multiplier; higher is louder.
        .param("gate", 1.0) // units: trigger, range: 0..1, default: 1.0. Gate or trigger input; positive edges fire the event or advance the clock.
        .body(|freq, amp, gate| {
            let env = envelope()
                .asr(0.06, 1.0, 0.12)
                .gate(gate)
                .cleanup_on_finish()
                .build();

            // Filter envelope
            let filt_env = envelope()
                .asr(0.03, 1.0, 0.15)
                .gate(gate)
                .build();

            // Two saw oscillators
            let osc_a = saw_ar(freq);
            let osc_b = saw_ar(freq * 1.005);

            let mix = (osc_a + osc_b) * 0.5;

            // Bright filter sweep
            let cutoff = 500.0 + filt_env * 4000.0;
            let filtered = rlpf_ar(mix, cutoff, 0.35);

            filtered * env * amp
        })
});

// Prophet Lead - Screaming lead
define_synthdef("prophet_lead", |builder| {
    builder
        .param("freq", 440.0) // units: Hz, range: 20..20000, default: 440.0. Pitch frequency in Hz; higher raises the note or resonator center.
        .param("amp", 0.5) // units: linear, range: 0..1, default: 0.5. Output level multiplier; higher is louder.
        .param("gate", 1.0) // units: trigger, range: 0..1, default: 1.0. Gate or trigger input; positive edges fire the event or advance the clock.
        .param("resonance", 0.4) // units: resonance, range: 0..1, default: 0.4. Filter resonance amount; higher emphasizes a narrower frequency peak.
        .body(|freq, amp, gate, resonance| {
            let env = envelope()
                .asr(0.01, 1.0, 0.15)
                .gate(gate)
                .cleanup_on_finish()
                .build();

            // Poly-mod style oscillator sync simulation
            let osc_a = saw_ar(freq);
            let osc_b = saw_ar(freq * 2.01); // Slight detune for richness

            let mix = (osc_a + osc_b * 0.5) * 0.6;

            // Resonant filter
            let filtered = rlpf_ar(mix, 3000.0, 1.0 - resonance * 0.8);

            filtered * env * amp
        })
});

// Prophet Strings - Warm polysynth strings
define_synthdef("prophet_strings", |builder| {
    builder
        .param("freq", 440.0) // units: Hz, range: 20..20000, default: 440.0. Pitch frequency in Hz; higher raises the note or resonator center.
        .param("amp", 0.4) // units: linear, range: 0..1, default: 0.4. Output level multiplier; higher is louder.
        .param("gate", 1.0) // units: trigger, range: 0..1, default: 1.0. Gate or trigger input; positive edges fire the event or advance the clock.
        .body(|freq, amp, gate| {
            let env = envelope()
                .asr(0.25, 1.0, 0.4)
                .gate(gate)
                .cleanup_on_finish()
                .build();

            // Ensemble-style detuning
            let v1 = saw_ar(freq * 0.997);
            let v2 = saw_ar(freq * 0.999);
            let v3 = saw_ar(freq * 1.001);
            let v4 = saw_ar(freq * 1.003);

            let voices = (v1 + v2 + v3 + v4) * 0.3;

            // Warm filter
            let filtered = lpf_ar(voices, 3000.0);

            filtered * env * amp
        })
});

//
// # Usage: `prophet_pad`
//
// ```rhai
// import "stdlib/synths/prophet.vibe" as prophet;
//
// let prophet_pad = voice("doc_prophet_pad")
//     .synth("prophet_pad")
//     .set_param("freq", 440.0)
//     .set_param("amp", 0.5)
//     .set_param("gate", 1.0)
//     .set_param("spread", 0.5);
// prophet_pad.output("out").to_main();
// prophet_pad.run();
// ```
//
// # Usage: `prophet_brass`
//
// ```rhai
// import "stdlib/synths/prophet.vibe" as prophet;
//
// let prophet_brass = voice("doc_prophet_brass")
//     .synth("prophet_brass")
//     .set_param("freq", 440.0)
//     .set_param("amp", 0.5)
//     .set_param("gate", 1.0);
// prophet_brass.output("out").to_main();
// prophet_brass.run();
// ```
//
// # Usage: `prophet_lead`
//
// ```rhai
// import "stdlib/synths/prophet.vibe" as prophet;
//
// let prophet_lead = voice("doc_prophet_lead")
//     .synth("prophet_lead")
//     .set_param("freq", 440.0)
//     .set_param("amp", 0.5)
//     .set_param("gate", 1.0)
//     .set_param("resonance", 0.4);
// prophet_lead.output("out").to_main();
// prophet_lead.run();
// ```
//
// # Usage: `prophet_strings`
//
// ```rhai
// import "stdlib/synths/prophet.vibe" as prophet;
//
// let prophet_strings = voice("doc_prophet_strings")
//     .synth("prophet_strings")
//     .set_param("freq", 440.0)
//     .set_param("amp", 0.5)
//     .set_param("gate", 1.0);
// prophet_strings.output("out").to_main();
// prophet_strings.run();
// ```
