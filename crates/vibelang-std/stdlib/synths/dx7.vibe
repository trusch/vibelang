// Yamaha DX7 Emulation
// Classic 1983 | Character: Crystalline, bell-like, FM synthesis
//
// The bestselling synth of the 80s. FM synthesis brought
// electric pianos, bells, and metallic tones to the masses.

// DX7 Electric Piano - The sound of the 80s
define_synthdef("dx7_epiano", |builder| {
    builder
        .param("freq", 440.0) // units: Hz, range: 20..20000, default: 440.0. Pitch frequency in Hz; higher raises the note or resonator center.
        .param("amp", 0.5) // units: linear, range: 0..1, default: 0.5. Output level multiplier; higher is louder.
        .param("gate", 1.0) // units: trigger, range: 0..1, default: 1.0. Gate or trigger input; positive edges fire the event or advance the clock.
        .param("brightness", 0.6) // units: normalized, range: 0..1, default: 0.6. Tone brightness; higher values add more high-frequency content.
        .body(|freq, amp, gate, brightness| {
            let env = envelope()
                .asr(0.005, 1.0, 0.3)
                .gate(gate)
                .cleanup_on_finish()
                .build();

            // FM modulation envelope (fast attack, medium decay)
            let mod_env = envelope()
                .perc(0.001, 0.3)
                .build();

            // 2-operator FM synthesis
            let mod_depth = brightness * 3.0 * mod_env + 0.5;
            let modulator = sin_osc_ar(freq * 14.0) * mod_depth;
            let carrier = sin_osc_ar(freq + modulator);

            // Second partial for richness
            let mod2 = sin_osc_ar(freq * 7.0) * (brightness * 1.5 * mod_env);
            let carrier2 = sin_osc_ar(freq * 2.0 + mod2) * 0.4;

            // Third partial
            let carrier3 = sin_osc_ar(freq * 3.0) * mod_env * 0.2;

            let fm = carrier + carrier2 + carrier3;

            fm * env * amp * 0.5
        })
});

// DX7 Bass - Punchy FM bass
define_synthdef("dx7_bass", |builder| {
    builder
        .param("freq", 110.0) // units: Hz, range: 20..20000, default: 110.0. Pitch frequency in Hz; higher raises the note or resonator center.
        .param("amp", 0.6) // units: linear, range: 0..1, default: 0.6. Output level multiplier; higher is louder.
        .param("gate", 1.0) // units: trigger, range: 0..1, default: 1.0. Gate or trigger input; positive edges fire the event or advance the clock.
        .body(|freq, amp, gate| {
            let env = envelope()
                .asr(0.003, 1.0, 0.1)
                .gate(gate)
                .cleanup_on_finish()
                .build();

            // Sharp FM modulation envelope
            let mod_env = envelope()
                .perc(0.001, 0.15)
                .build();

            // FM with ratio for bass punch
            let mod_depth = mod_env * 4.0 + 0.3;
            let modulator = sin_osc_ar(freq * 1.0) * mod_depth * freq;
            let carrier = sin_osc_ar(freq + modulator);

            // Sub for weight
            let sub = sin_osc_ar(freq * 0.5) * 0.4;

            (carrier * 0.7 + sub) * env * amp
        })
});

// DX7 Bells - Classic FM bells
define_synthdef("dx7_bells", |builder| {
    builder
        .param("freq", 880.0) // units: Hz, range: 20..20000, default: 880.0. Pitch frequency in Hz; higher raises the note or resonator center.
        .param("amp", 0.4) // units: linear, range: 0..1, default: 0.4. Output level multiplier; higher is louder.
        .param("gate", 1.0) // units: trigger, range: 0..1, default: 1.0. Gate or trigger input; positive edges fire the event or advance the clock.
        .body(|freq, amp, gate| {
            let env = envelope()
                .asr(0.001, 1.0, 0.5)
                .gate(gate)
                .cleanup_on_finish()
                .build();

            // Long decay for bell
            let decay_env = envelope()
                .perc(0.001, 2.0)
                .build();

            // Inharmonic FM ratios for bell character
            let mod1 = sin_osc_ar(freq * 3.5) * decay_env * 2.0;
            let carrier1 = sin_osc_ar(freq + mod1);

            let mod2 = sin_osc_ar(freq * 7.1) * decay_env * 1.5;
            let carrier2 = sin_osc_ar(freq * 2.0 + mod2) * 0.5;

            let mod3 = sin_osc_ar(freq * 11.0) * decay_env * 0.8;
            let carrier3 = sin_osc_ar(freq * 4.0 + mod3) * 0.3;

            let bell = carrier1 + carrier2 + carrier3;

            bell * env * decay_env * amp * 0.5
        })
});

// DX7 Brass - Punchy FM brass
define_synthdef("dx7_brass", |builder| {
    builder
        .param("freq", 220.0) // units: Hz, range: 20..20000, default: 220.0. Pitch frequency in Hz; higher raises the note or resonator center.
        .param("amp", 0.5) // units: linear, range: 0..1, default: 0.5. Output level multiplier; higher is louder.
        .param("gate", 1.0) // units: trigger, range: 0..1, default: 1.0. Gate or trigger input; positive edges fire the event or advance the clock.
        .body(|freq, amp, gate| {
            let env = envelope()
                .asr(0.05, 1.0, 0.1)
                .gate(gate)
                .cleanup_on_finish()
                .build();

            // Brass-like FM modulation
            let mod_env = envelope()
                .asr(0.02, 1.0, 0.15)
                .gate(gate)
                .build();

            let mod_depth = mod_env * 2.5 + 0.5;
            let modulator = sin_osc_ar(freq * 1.0) * mod_depth;
            let carrier = sin_osc_ar(freq + modulator);

            // Higher harmonic
            let mod2 = sin_osc_ar(freq * 3.0) * mod_env * 1.5;
            let carrier2 = sin_osc_ar(freq * 2.0 + mod2) * 0.4;

            let brass = carrier + carrier2;

            brass * env * amp * 0.6
        })
});

// DX7 Marimba - Classic FM marimba
define_synthdef("dx7_marimba", |builder| {
    builder
        .param("freq", 440.0) // units: Hz, range: 20..20000, default: 440.0. Pitch frequency in Hz; higher raises the note or resonator center.
        .param("amp", 0.5) // units: linear, range: 0..1, default: 0.5. Output level multiplier; higher is louder.
        .param("gate", 1.0) // units: trigger, range: 0..1, default: 1.0. Gate or trigger input; positive edges fire the event or advance the clock.
        .body(|freq, amp, gate| {
            let env = envelope()
                .asr(0.001, 1.0, 0.1)
                .gate(gate)
                .cleanup_on_finish()
                .build();

            // Fast decay for mallet sound
            let decay_env = envelope()
                .perc(0.001, 0.5)
                .build();

            // FM for woody attack
            let mod_depth = decay_env * 3.0;
            let modulator = sin_osc_ar(freq * 4.0) * mod_depth;
            let carrier = sin_osc_ar(freq + modulator);

            // Fundamental
            let fundamental = sin_osc_ar(freq) * 0.6;

            (carrier * 0.5 + fundamental) * env * decay_env * amp
        })
});

//
// # Usage: `dx7_epiano`
//
// ```rhai
// import "stdlib/synths/dx7.vibe" as dx7;
//
// let dx7_epiano = voice("doc_dx7_epiano")
//     .synth("dx7_epiano")
//     .set_param("freq", 440.0)
//     .set_param("amp", 0.5)
//     .set_param("gate", 1.0)
//     .set_param("brightness", 0.6);
// dx7_epiano.output("out").to_main();
// dx7_epiano.run();
// ```
//
// # Usage: `dx7_bass`
//
// ```rhai
// import "stdlib/synths/dx7.vibe" as dx7;
//
// let dx7_bass = voice("doc_dx7_bass")
//     .synth("dx7_bass")
//     .set_param("freq", 440.0)
//     .set_param("amp", 0.5)
//     .set_param("gate", 1.0);
// dx7_bass.output("out").to_main();
// dx7_bass.run();
// ```
//
// # Usage: `dx7_bells`
//
// ```rhai
// import "stdlib/synths/dx7.vibe" as dx7;
//
// let dx7_bells = voice("doc_dx7_bells")
//     .synth("dx7_bells")
//     .set_param("freq", 440.0)
//     .set_param("amp", 0.5)
//     .set_param("gate", 1.0);
// dx7_bells.output("out").to_main();
// dx7_bells.run();
// ```
//
// # Usage: `dx7_brass`
//
// ```rhai
// import "stdlib/synths/dx7.vibe" as dx7;
//
// let dx7_brass = voice("doc_dx7_brass")
//     .synth("dx7_brass")
//     .set_param("freq", 440.0)
//     .set_param("amp", 0.5)
//     .set_param("gate", 1.0);
// dx7_brass.output("out").to_main();
// dx7_brass.run();
// ```
//
// # Usage: `dx7_marimba`
//
// ```rhai
// import "stdlib/synths/dx7.vibe" as dx7;
//
// let dx7_marimba = voice("doc_dx7_marimba")
//     .synth("dx7_marimba")
//     .set_param("freq", 440.0)
//     .set_param("amp", 0.5)
//     .set_param("gate", 1.0);
// dx7_marimba.output("out").to_main();
// dx7_marimba.run();
// ```
