// kind=fx | category=filter | flavor=ladder | stereo=true | params=cutoff,res,mix,level
//
// Ladder Filter Effect
//
// Moog-style ladder filter with resonance.
//
// Usage:
//   group.add_effect("lad", "ladder_filter", #{
//     cutoff: 1000.0,
//     res: 0.5,
//     mix: 1.0,
//     level: 1.0
//   });
//
// Parameters:
//   cutoff: Filter cutoff frequency (50 to 15000 Hz)
//   res:    Resonance (0.0 to 1.0)
//   mix:    Dry/wet mix (0.0 = dry, 1.0 = wet)
//   level:  Output trim applied after the dry/wet mix

define_fx("ladder_filter", |builder| {
    builder
        .param("cutoff", 1000.0) // units: Hz, range: 20..20000, default: 1000.0. Filter cutoff or tone point; higher keeps more high-frequency brightness.
        .param("res", 0.5) // units: resonance, range: 0..1, default: 0.5. Filter resonance amount; higher values emphasize the cutoff peak.
        .param("mix", 1.0) // units: ratio, range: 0..1, default: 1.0. Dry/wet balance; 0 is dry and 1 is fully processed.
        .param("level", 1.0) // units: linear, range: 0..1, default: 1.0. Output level multiplier; higher is louder.
        .body(|input, cutoff, res, mix, level| {
            // Multi-stage lowpass for ladder characteristic
            let stage1_left = lpf_ar(input[0], cutoff);
            let stage2_left = lpf_ar(stage1_left, cutoff);
            let stage3_left = lpf_ar(stage2_left, cutoff);
            let stage4_left = lpf_ar(stage3_left, cutoff);

            let stage1_right = lpf_ar(input[1], cutoff);
            let stage2_right = lpf_ar(stage1_right, cutoff);
            let stage3_right = lpf_ar(stage2_right, cutoff);
            let stage4_right = lpf_ar(stage3_right, cutoff);

            // Add resonance feedback
            let feedback = res * 3.8;
            let res_left = stage4_left + input[0] * feedback * 0.1;
            let res_right = stage4_right + input[1] * feedback * 0.1;

            let wet_l = leak_dc_ar(res_left, 0.995);
            let wet_r = leak_dc_ar(res_right, 0.995);

            [
                ((1.0 - mix) * input[0] + mix * wet_l) * level,
                ((1.0 - mix) * input[1] + mix * wet_r) * level
            ]
        })
});

//
// # Usage: ladder_filter
//
// ```rhai
// import "stdlib/effects/filters/ladder_filter.vibe" as ladder_filter;
//
// let doc_ladder_filter = fx("doc_ladder_filter")
//     .synth("ladder_filter")
//     .param("cutoff", 1000.0)
//     .param("res", 0.5)
//     .param("mix", 1.0)
//     .param("level", 1.0)
//     .apply();
// ```
